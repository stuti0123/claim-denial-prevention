# ============================================================
# Requirements — AI-Powered Claim Denial Prevention System
# ============================================================
# Install: pip install -r requirements.txt
# Python:  3.11+

# ---- Core data engineering (Weeks 1-4) ---------------------
pandas>=2.0.0
numpy>=1.24.0

# ---- Machine learning (Week 5) -----------------------------
scikit-learn>=1.3.0
xgboost>=2.0.0          # denial prediction model
optuna>=3.0.0           # hyperparameter tuning
shap>=0.44.0            # per-claim explainability (SHAP values)
joblib>=1.3.0           # model serialisation (save/load .pkl)

# ---- RAG system (Week 6) -----------------------------------
sentence-transformers>=2.6.0   # local embeddings (no API cost)
faiss-cpu>=1.7.4               # local vector store (no SaaS cost)
boto3>=1.34.0                  # AWS Bedrock client (LLM generation)

# ---- API backend (Week 8) ----------------------------------
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0         # request/response schema validation

# ---- Dashboard frontend (Weeks 2 & 8) ----------------------
streamlit>=1.28.0
requests>=2.31.0        # UI → API HTTP calls
altair>=5.0.0           # charts in Streamlit dashboard

# ---- Visualisation (profiling / notebooks) ------------------
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- Environment & secrets ---------------------------------
python-dotenv>=1.0.0   # loads .env into os.environ

# ---- Testing -----------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0      # coverage reporting

# ---- Spark / Delta (Databricks cloud runs only) ------------
# Uncomment when running on Databricks — not needed locally
# pyspark>=3.3.0
# delta-spark>=2.3.0
